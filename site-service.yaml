apiVersion: v1
kind: Service

metadata:
  name: my-website-service
  labels:
    app: my-website

spec:
  selector:
    app: my-website
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer